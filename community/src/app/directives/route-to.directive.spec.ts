import { RouteToDirective } from './route-to.directive';

describe('RouteToDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteToDirective();
    expect(directive).toBeTruthy();
  });
});
